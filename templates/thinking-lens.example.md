---
title: Thinking Lens — how the owner thinks (the judgment filter)
type: self
tags: [self, judgment, thinking, always-relevant]
ai-first: true
created: 2026-07-02
updated: 2026-07-02
sources: [bootstrap]
---

# Thinking Lens — how the owner thinks

> **For future Claude:** This page is the **judgment filter** for the whole memory system. Whenever any input is processed — an article, a session, a conversation — do not stop at *what the source says*. Run it through this lens and extract *what the owner should learn and apply*, in their voice, given who they are. Durable outputs go to `principles/`.

## The thinking modes (name each one; ground each in a REAL example from the owner's history)

1. **<Mode name — e.g. First-principles / mechanism-first.>** <One-line definition.> Evidence: <a real project/session where the owner thought this way>.
2. **<Mode name — e.g. Builder / ship-to-learn.>** …
3. **<Mode name — e.g. Forward / second-order.>** …
4. **<Mode name — e.g. Evidence over appearance.>** …

*(4–6 modes. If you cannot ground a mode in a real example, it does not belong here — the lens must be who the owner actually is, not who they aspire to be.)*

## Identity vectors (what judgments must be conditioned on)

- **<Vector — e.g. Aspiring founder in <domain>>** — inputs about startups/markets → read as *operating instructions for my future venture*.
- **<Vector — e.g. Writer with a public brand>** — inputs about content/credibility → read as brand and integrity rules.
- **<Vector — e.g. Operator at <company>>** — inputs about the industry → read with operator instincts.

*(Keep current — retire vectors when life changes; the synthesis loop should update this page like any other.)*

## How to apply the lens (the extraction procedure)

For every input, after capturing what it says:
1. Which identity vector(s) does this touch?
2. Through the thinking modes: what is the mechanism underneath the story? What would the owner *do differently* going forward?
3. Phrase the learning as a first-person prescription with a trigger: "When I <situation>, I <rule>, because <why>."
4. Durable (matters in 6+ months, applies beyond this one source)? → create/update `principles/<slug>.md` with the source as provenance. One-off → note the judgment inline on the source/session page only. **Durability is the ONLY promotion gate — do not invent stricter ones (e.g. "wait until it recurs"). If it passes, promote now; a later source strengthens provenance, it is not a precondition.**
5. Conflicts with an existing principle? Flag the tension; never silently overwrite.

**Failure mode to avoid:** storing the knowledge ("bots leave telltales") while dropping the judgment ("therefore I never let telltales through"). The judgment IS the payload; the knowledge is context.
